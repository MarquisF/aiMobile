import React, { Component } from 'react';

export default class Upcoming extends Component {
  constructor( props ) {
    super( props );
  }

  render() {
    return (
      <div>
        Upcoming
      </div>
    )
  }
}